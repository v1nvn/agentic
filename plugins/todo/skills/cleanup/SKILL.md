---
name: cleanup
description: Tracking hygiene — archive sweep of landed threads, log realign of stale thread files, index/progress divergence repair, evaporated-deferral detection. Use when the user asks for tracking cleanup, an archive sweep, or index repair; optional, never required.
argument-hint: (no args)
---

Optional hygiene, never required — archive sweep of threads whose runs landed pre-adoption, log realign of pre-adoption thread files, index/`progress/` divergence repair, evaporated-deferral detection. Nothing waits on this verb. Scaffold: the full procedure is not written yet.
