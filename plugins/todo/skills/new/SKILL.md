---
name: new
description: Start a work-tracking thread at session end — the TODO.md index line always, progress/<slug>.md when the discussion carries enough to fill it. Use when the session opened a new thread of work ("start a thread on X", "add a line for X") or at end of session when a thread start is owed.
argument-hint: [title]
---

Start a thread at session end — a `TODO.md` index line always; `progress/<slug>.md` only when the discussion carries enough to fill it. A given `[title]` is used; a skipped one is derived from what the session discussed. Scaffold: the full procedure is not written yet.
